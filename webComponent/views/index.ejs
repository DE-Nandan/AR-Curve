<div class="home">
	<h1><%= title %></h1>
	<a href="/logout">Logout</a>
</div>

<div>
	<button>
		<script>
			if(!use.favourite.find()==null) 
				usr.favourite.add(image.sno); 
			else 
				usr.favourite.remove(image.sno);
		</script>
		Favourite
	</button>
   
</div>